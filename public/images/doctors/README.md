Doctor image slots:

- `dr-adebayo-clinical.svg` is the clinical equipment photo slot.
- `dr-adebayo-portrait.svg` is the seated portrait photo slot.

Replace these with JPG or PNG files using the same base names if you want the exact supplied photographs rendered by Next Image.
